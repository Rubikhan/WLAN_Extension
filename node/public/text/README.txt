Make sure that there exist five files: 
	line1, 
	line2, 
	line3, 
	line4, 
	line5
Otherwise the extension might not work.

If you changed the number of lines in the source, then make sure that:
* the number of files here matches it
* the number of paragraphs in index.html matches it